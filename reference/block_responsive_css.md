# Generate responsive CSS for blockr blocks

Creates CSS for responsive grid layout using 'block-' prefix. Can be
reused across different blockr packages.

## Usage

``` r
block_responsive_css()
```

## Value

HTML style tag with responsive CSS
