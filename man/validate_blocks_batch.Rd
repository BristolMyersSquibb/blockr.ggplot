% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate-screenshot.R
\name{validate_blocks_batch}
\alias{validate_blocks_batch}
\title{Batch validate multiple blocks with screenshots}
\usage{
validate_blocks_batch(
  blocks,
  data = datasets::mtcars,
  output_dir = "man/figures",
  verbose = TRUE
)
}
\arguments{
\item{blocks}{Named list of blocks to validate}

\item{data}{Data to use for all blocks (can also be a named list
matching block names)}

\item{output_dir}{Directory to save screenshots (default: "man/figures")}

\item{verbose}{Print progress messages (default: TRUE)}
}
\value{
A data frame with validation results for each block
}
\description{
Convenience function to validate multiple blocks at once and generate
a summary report of which blocks work and which don't.
}
\examples{
\dontrun{
# Test multiple blocks
blocks <- list(
  scatter = new_chart_block(type = "point", x = "wt", y = "mpg"),
  bar = new_chart_block(type = "bar", x = "cyl"),
  histogram = new_chart_block(type = "histogram", x = "mpg")
)

results <- validate_blocks_batch(blocks)
print(results)

# With different data for each block
blocks <- list(
  iris_scatter = new_chart_block(
    type = "point",
    x = "Sepal.Length",
    y = "Sepal.Width"
  ),
  mtcars_bar = new_chart_block(type = "bar", x = "cyl")
)

data_list <- list(
  iris_scatter = iris,
  mtcars_bar = datasets::mtcars
)

results <- validate_blocks_batch(blocks, data = data_list)
}

}
